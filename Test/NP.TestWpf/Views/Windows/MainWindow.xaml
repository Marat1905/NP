<Window x:Class="NP.TestWpf.Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NP.TestWpf.Views.Windows"
        mc:Ignorable="d"
        DataContext="{Binding MainWindowModel, Source={StaticResource Locator}}"
        Title="{Binding Title}" Height="450" Width="1250">
    <DockPanel Margin="10">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="20"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <ToggleButton Grid.Column="0" Grid.Row="0"  Content="Имитация обрыва" Style="{StaticResource ToggleButtonStyle1}" Command="{Binding BreakCommand}" IsChecked="{Binding Break}" />
            <TextBox Grid.Column="0"  Grid.Row="1"  Text="{Binding Break}" Height="20" IsEnabled="False" Margin="0,0,10,0"/>
            <DataGrid ItemsSource="{Binding BreakList}"  Grid.Column="1" Grid.Row="0" Grid.RowSpan="3"/>
        </Grid>
       
    </DockPanel>
</Window>
